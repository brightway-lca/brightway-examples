# https://docs.conda.io/projects/conda/en/latest/user-guide/tasks/manage-environments.html#create-env-file-manually
# install this conda environment using the command:
# conda env create -f template.yaml
name: main
channels:
  - conda-forge
  - nodefaults
dependencies:
  # core functionality
  - python=3.10
  - jupyterlab
  # data science
  - scipy
  - pandas
  # brightway packages
  - bw2io
  - bw2data
  - bw2calc